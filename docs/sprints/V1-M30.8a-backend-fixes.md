# V1-M30.8a: Backend Bugs Hotfix (P0 + P1, low-risk)

> **Status:** 🔴 RED — RED tests pending, awaiting nodejs-dev + rust-dev
> **Branch:** `feature/V1-M30.8a-backend-fixes` (from dev post-V1-M30.7, parallel with V1-M30.8b)
> **Created:** 2026-04-30
> **Author:** Architect
> **Triggered by:** Phase 1 deep-dive into scan/eval/fix/passport CLI outputs found 8 backend bugs not surfaced in HTML-only verification. User chose Option 1: split into 2 parallel hotfixes.
> **Predecessor:** V1-M30.7 rendering-bugs
> **Sibling:** V1-M30.8b eval-quality-ux (parallel branch)
> **Successor:** Final /deep-e2e + version bump → tag v1.0.0

---

## 1. Goal

Phase 1 deep-dive (reading actual scan/eval/fix/passport CLI outputs across 3 profiles)
surfaced 8 backend bugs that did NOT show up in the 172/172 atomic HTML checks. They
are all small to medium effort, low regression risk. This hotfix closes them in one
branch so V1-M30.8b (eval pattern matcher + UX) can run in parallel.

## 2. Bugs in scope

| # | Bug | FA | Severity | Evidence |
|---|---|---|---|---|
| **W-1** | 6 user-facing `complior passport list/show/...` strings in Rust CLI not updated | FA-04 | P0 | `commands.rs:608`, `format/report.rs:458,464`, `passport.rs:401,560,2225` |
| **W-2** | Daemon log error: `Agent 'eval-target-anthropic' not found` — looks for `<name>.json`, files saved as `<name>-manifest.json` | FA-04 | P0 | `daemon.log` line: `ENOENT: ... '/.../eval-target-anthropic.json'` |
| **W-3** | Scan finding fix hint says `complior docs generate --missing` — `docs` command does NOT exist in v1.0.0 | FA-01 | P0 | `domain/fixer/strategies/doc-code-sync.ts:30`. Verified: `complior docs --help` → `error: unrecognized subcommand 'docs'` |
| **W-4** | Scan score inconsistency: `complior scan` CLI prints `75/100`, but `report.json:summary.scanScore = 65` (10-point delta) | FA-01 §5.4 | P1 | scan_json.log totalScore=75 vs report.json scanScore=65 |
| **W-5** | `l5-doc-fria` HIGH severity finding for Profile A (limited risk) — V1-M30.6 fixed Documents tab display, but scanner L5 doc validator still runs FRIA validation regardless of profile risk | FA-01 §4.11, §12 | P1 | Profile A findings include `l5-doc-fria HIGH` though FRIA is high-risk-only per Art. 27 |
| **W-6** | `undeclared-permission` reports "No passport found" — but project DOES have 2 passports in `.complior/agents/` | FA-01 §4.1 | P1 | Profile A findings show `undeclared-permission info` "No passport found" while `eval-target-anthropic-manifest.json` and `eval-target-openai-manifest.json` both exist |
| **W-7** | Fix dry-run output: `FILES TO CREATE` list contains files that already exist as `draft` status. The fix planner shows "create" for existing files | FA-03 §2 (Discovery phase) | P1 | `fix_all.log`: 5 files in "FILES TO CREATE" but Documents tab shows them all as `draft` (existing on disk) |
| **W-8** | Fix score estimate `65 → ~100` in `fix.rs:517` — `format!("{before:.0} → ~{after:.0}")` with hardcoded `~` prefix. The `after` is overly optimistic (predicting all fixes succeed perfectly to give 100) | FA-03 §2 (Planning preview) | P2 | `fix_all.log`: `SCORE (estimated)  65 → ~100`. Realistic delta depends on which fixes actually apply correctly. |

## 3. Tasks Table

| ID | Task | Owner | RED test target |
|----|------|-------|-----------------|
| **W-1** | Replace 6× `complior passport <verb>` user-facing strings with `complior agent <verb>`. The deprecation warning string in `main.rs:362` MUST be preserved (it explicitly references the deprecated alias). | rust-dev | extend `cli/src/headless/tests.rs::user_facing_init_hints_use_agent_not_passport` to also forbid `complior passport list`, `complior passport show`, `complior passport fria`, etc. — full alias coverage. New test: `user_facing_passport_subcommand_hints_use_agent` |
| **W-2** | Fix passport file-lookup bug. Find the call site that uses `${name}.json` instead of `${name}-manifest.json` and align with the rest of `passport-service.ts` which correctly uses the suffix. | nodejs-dev | new test `passport-service-lookup-uses-manifest-suffix.test.ts` — assert `loadPassport(name)` and any agent-fetch handler reads from `${name}-manifest.json`, never bare `${name}.json` |
| **W-3** | Replace `complior docs generate --missing` (and any other `complior docs ...` literal) with a valid v1.0.0 command, e.g. `complior fix --doc <type>` or `complior fix` | nodejs-dev | new test `no-invalid-complior-docs-hints.test.ts` — source-introspection over `engine/core/src/domain/fixer/strategies/` and `engine/core/src/domain/scanner/finding-explainer.ts` (and any other location): no occurrence of `complior docs` literal |
| **W-4** | Reconcile the scan score divergence. Either (a) make `summary.scanScore` use `scanResult.score.totalScore` directly, OR (b) document that `summary.scanScore` is post-profile-filter (lower) while CLI shows pre-filter (raw). **Choose (a)** — single source of truth. | nodejs-dev | new test `scan-score-consistent.test.ts` — fixture with known scan result → both `scanResult.score.totalScore` AND `summary.scanScore` produce identical value (same number) |
| **W-5** | Apply risk-level filter to L5 doc validation findings. In `layer5-docs.ts` (or wherever L5 doc findings are generated), if profile.riskLevel is not `high`/`unacceptable` AND the doc is FRIA (Art. 27), the finding should be `type: 'skip'` with message "Skipped: FRIA only required for high-risk profiles". | nodejs-dev | new test `l5-doc-fria-skips-for-limited.test.ts` — fixture with profile=limited + FRIA doc present → L5 finding is `type: 'skip'`, NOT `'fail'` |
| **W-6** | `undeclared-permission` check (passport-presence or permission-scanner) must use the same agent-listing logic as `passport-service.listPassports()`. Fix the lookup in `passport-presence.ts` or `permission-scanner.ts` to enumerate `*-manifest.json` files. | nodejs-dev | new test `undeclared-permission-finds-existing-passports.test.ts` — fixture with `eval-target-foo-manifest.json` in `.complior/agents/` → `permission-scanner` does NOT report "No passport found" |
| **W-7** | Fix planner Discovery phase must check if `outputFile` exists on disk. If the file exists (regardless of content quality), classify as **enrich** (not **create**), so dry-run output shows `[ENRICH]` instead of `[PREVIEW] (will create)`. | nodejs-dev | new test `fix-discovery-detects-existing-files.test.ts` — fixture with existing `docs/compliance/foo.md` (draft) + matching fix plan → action.type = `enrich` not `create` |
| **W-8** | Realistic fix score estimate: replace `~{after:.0}` with computed estimate based on fix priorities × per-fix score impact. If unable to compute, use min(99, before + sum_of_score_impacts) instead of a fixed 100. The `~` prefix is OK to keep (still an estimate). | rust-dev | extend `cli/src/headless/tests.rs` — assert that the formula in `fix.rs` does NOT hardcode `100`, and the test fixture with a 65 baseline + 3 fixes (5+8+8 impact) produces estimate = 86 (not 100) |

## 4. Architecture Requirements

- All RED tests GREEN
- DO NOT touch other V1-M30.* tests (only ADD new tests in this milestone)
- Pure functions stay pure
- `Object.freeze` on any new shared constants
- W-3: keep `complior fix --doc <type>` syntax accurate per FA-03 §3.1 (`complior fix --doc fria/notify/policy/...`)
- W-5: filter logic must use the existing `risk-level-filter.ts` patterns — DO NOT duplicate
- W-7: fix planner detection should follow `FixPlan` types in FA-03 (action types: create, edit, splice, generate)
- DO NOT touch Rust CLI **except** for W-1 and W-8 (rust-dev tasks)
- DO NOT touch test files for V1-M30.{1..7} milestones

## 5. Acceptance Criteria

- [ ] All 8 RED tests GREEN
- [ ] /deep-e2e re-run shows:
  - 0× `complior passport list/show/...` in any HTML report or CLI stdout
  - daemon.log clean (no ENOENT for missing `<name>.json`)
  - `complior scan` CLI score == `report.json:summary.scanScore`
  - Profile A findings: NO `l5-doc-fria` finding (or it's `type: skip` not `fail`)
  - Profile A: NO `undeclared-permission "No passport found"` (passports present)
  - Fix dry-run: `[ENRICH]` for existing files, `[PREVIEW] (create)` only for missing
  - Fix score estimate: realistic value, not always 100
- [ ] dev CI green (all 9 jobs)
- [ ] Full TS suite stays GREEN (2465+ pass)
- [ ] Full Rust suite stays GREEN (215+ pass)
- [ ] tsc + cargo fmt + clippy clean

## 6. Out of Scope (in V1-M30.8b)

- Bug #5 (eval false negatives — pattern matcher)
- Bug #8-11 (UX polish: action title length, doc Modified, disclaimer prose, cross-domain)

## 7. Handoff

Architect commits spec + 8 RED tests + STOPS. User spawns nodejs-dev for W-2..W-7,
then rust-dev for W-1 + W-8 (or both in parallel). After both done, user spawns
reviewer. After APPROVED → user merges. Then check V1-M30.8b status before final
/deep-e2e.
