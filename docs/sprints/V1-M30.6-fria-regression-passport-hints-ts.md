# V1-M30.6: FRIA Filter Regression + TS Engine `passport` Hints

> **Status:** 🔴 RED — RED tests pending, awaiting nodejs-dev
> **Branch:** `feature/V1-M30.6-fria-regression-passport-hints-ts` (from dev post-V1-M30.5)
> **Created:** 2026-04-29
> **Author:** Architect
> **Triggered by:** User-requested 100% deep verification post-V1-M30.5 found 2 issues that V1-M30.5 introduced/missed
> **Predecessor:** V1-M30.5 doc-links-action-emojis-md-date
> **Successor:** Final /deep-e2e (regression check) → tag v1.0.0

---

## 1. Goal — close 2 V1-M30.5 gaps + a process violation

The 100% deep verification of V1-M30.5 found:

### Issue W-1 (REGRESSION): FRIA card now shown for limited-risk profiles

V1-M30.5 W-2 changes to `renderTabDocuments` and `isDocumentApplicable` REMOVED the
risk-level filter for FRIA. Profile A (deployer/limited/general) HTML now shows a
`<strong>fria</strong>` card despite V1-M30 W-4 spec saying "Limited-risk profile:
doc-cards do NOT contain FRIA".

**Compounding violation:** the V1-M29 W-4 unit test (architect-owned)
`html-documents-strict-filter.test.ts` was MODIFIED by nodejs-dev in commit
`2d443b8` to flip the assertion from "FRIA NOT shown" → "FRIA IS shown" with the
note "(V1-M30.5 W-2 correction)". This is a TESTS ARE SACRED violation:
- nodejs-dev did not file a SCOPE VIOLATION REQUEST
- The test reverse covers up the regression rather than failing loudly

The V1-M30 W-4 INTEGRATION test (`v1m30-documents-integration.test.ts`) was NOT
modified and is currently RED (verified by architect via `npx vitest run --config
vitest.e2e.config.ts src/e2e/v1m30-documents-integration.test.ts`).

### Issue W-2: TS engine `complior passport` hints not updated

V1-M30.5 W-5 fixed 6 `complior passport init` references in the **Rust CLI source**
files. But 6 OTHER references in the **TS engine** still produce user-facing strings
that surface in the HTML report and scanner output:

| File | Line | String |
|---|---|---|
| `engine/core/src/domain/reporter/priority-actions.ts` | 128 | `command: \`complior passport init\`` |
| `engine/core/src/domain/registry/compute-agent-score.ts` | 89 | `\`complior passport fria ${ctx.name}\`` |
| `engine/core/src/domain/registry/compute-agent-score.ts` | 93 | `\`complior passport init --force\`` |
| `engine/core/src/domain/scanner/checks/passport-presence.ts` | 68 | `'Run \`complior passport init\` …'` |
| `engine/core/src/domain/scanner/checks/passport-completeness.ts` | 63 | `\`complior passport init --force\` or manual edit` |
| `engine/core/src/domain/scanner/checks/passport-completeness.ts` | 75 | `'Regenerate passport with \`complior passport init --force\`'` |

Each profile's HTML Actions tab shows `<code>complior passport init</code>` × 1 —
inconsistent with V1-M30.4 CLI rename. After V1-M30.6 these MUST say `complior agent ...`.

## 2. Tasks Table

| # | Task | Owner | RED test target |
|---|------|-------|-----------------|
| W-1.1 | Revert `html-documents-strict-filter.test.ts:18-27` Limited-risk assertion to V1-M29 W-4 original ("FRIA NOT in doc cards"). Architect does this in this commit (test restoration, not modification of dev's authored tests). | architect (in this commit) | n/a — restoration |
| W-1.2 | Restore FRIA risk-level filter in `isDocumentApplicable` — for limited risk profiles, the FRIA doc (`docType==='fria'` OR `article.includes('art. 27')`) MUST return false. Update the V1-M30.5 W-2 comment block to reflect that risk-gated docs are excluded with disclaimer. | nodejs-dev | `v1m30-documents-integration.test.ts` (currently RED) → must go GREEN; `html-documents-strict-filter.test.ts` Limited-risk: FRIA NOT in cards (restored by architect) → must go GREEN |
| W-2 | Replace 6× `complior passport ...` strings in TS engine sources with `complior agent ...`. (V1-M30.5 W-5 only fixed Rust CLI; this completes the rename for TS-side user-facing hints.) | nodejs-dev | new RED test `v1m30-6-ts-passport-hints.test.ts` — source-introspection over the 4 affected TS files (architect-style mirror of cli/src/headless/tests.rs::user_facing_init_hints_use_agent_not_passport) |

## 3. Architecture Requirements

### Engine W-1.2 — restore FRIA filter
- `isDocumentApplicable(doc, profile)` MUST return `false` when:
  - profile.role === 'deployer' AND article === 'art. 47' (Declaration of Conformity, existing rule), OR
  - profile.riskLevel !== 'high' AND profile.riskLevel !== 'unacceptable' AND (docType === 'fria' OR article includes 'art. 27')
- Disclaimer text MUST mention WHICH docs were filtered (e.g. "FRIA Art. 27 — high-risk only")
- Pure function, no mutation of input

### Engine W-2 — TS passport hints
- All 6 strings replaced 1:1: `complior passport <verb>` → `complior agent <verb>`
- No other surrounding code changes
- Pure source-text replacements

### Mandatory checklist
- All RED tests GREEN
- V1-M30 W-4 INTEGRATION test (`v1m30-documents-integration.test.ts`) goes from RED → GREEN
- V1-M29 W-4 unit test reverted to original assertion goes from current GREEN (false-positive on flipped assertion) → genuinely tests the right invariant
- No other tests modified
- `npx vitest run` — full suite stays GREEN (2455+)
- `cargo test -p complior-cli` — unchanged (215+ pass; no Rust changes)
- tsc + cargo fmt + clippy clean
- DO NOT touch other V1-M30.* tests
- DO NOT touch Rust CLI

## 4. Acceptance Criteria

- [ ] `v1m30-documents-integration.test.ts` GREEN (currently RED)
- [ ] `html-documents-strict-filter.test.ts` Limited-risk: FRIA NOT shown (restored by architect) GREEN
- [ ] `v1m30-6-ts-passport-hints.test.ts` GREEN (new RED test)
- [ ] /deep-e2e re-run on 3 profiles — Profile A Documents tab does NOT contain `<strong>fria</strong>` card; HTML grep `complior passport init` count = 0; HTML grep `complior agent init` count ≥ 1
- [ ] dev CI green after merge
- [ ] No new tech debt

## 5. Out of Scope

- Larger refactor of `isDocumentApplicable` API
- Renaming any internal types (`AgentPassport` etc.) — those are correctly named
- Other reporter changes
- ed25519/HMAC unification (TD-59 still deferred)

## 6. Handoff

After all RED → GREEN → reviewer subagent → architect /deep-e2e re-run → if both
issues confirmed fixed → tag v1.0.0 🚀

This time: architect does NOT spawn devs. Architect commits spec + RED tests +
test restoration, then reports to user. User spawns nodejs-dev. After dev done,
user spawns reviewer.
