# V1-M12.1: Eval Pre-Filter

**Status:** ✅ DONE
**Created:** 2026-04-21
**Completed:** 2026-04-21
**Reviewed:** APPROVED (with notes — TD-43, TD-44)
**Feature Area:** FA-02 (Eval Architecture) — see `docs/feature-areas/eval-architecture.md` §11
**Parent:** V1-M12 (Context-Aware Eval) — tech debt fix
**Goal:** Filter eval tests BEFORE execution (saves HTTP/LLM costs)

---

## Context

V1-M12 delivered profile filtering logic (`filterTestsByProfile`) and wired it into `eval-service.ts` — but the filter runs **AFTER** `runEval()` completes (lines 199-219). All tests still execute, costing the same HTTP/LLM calls. The filter only produces metadata (filterContext + disclaimer).

**This milestone moves filtering BEFORE execution** — the core cost-saving promise of V1-M12.

### What needs to change

In `eval-service.ts`, between lines 194 (profile loaded) and 196 (`runEval()` called):

1. Call `filterTestsByProfile()` on the test sources
2. Create a filtered `EvalTestSources` wrapper with only applicable tests
3. Pass the filtered sources to `runEval()` instead of unfiltered ones
4. Keep filterContext/disclaimer attachment (already done)

**~15-20 lines of code change.**

---

## Acceptance Criteria

- [x] `eval-service.ts` filters test sources BEFORE `runEval()` call
- [x] With deployer profile: provider-only tests (CT-8-*) produce zero HTTP calls
- [x] With deployer profile: GPAI tests (CT-10-*) absent from results
- [x] With healthcare profile: non-healthcare industry tests (CT-11-001..015) absent
- [x] Without profile: all tests execute (backward compatible)
- [x] HTTP call count = result.totalTests (no wasted calls)
- [x] 4 new tests GREEN (`eval-prefilter.test.ts`)
- [x] Existing 2257+ tests still GREEN (2261 total)
- [x] `tsc --noEmit` PASS

---

## Tasks

| # | Task | Agent | Method | Files |
|---|------|-------|--------|-------|
| T-1 | Filter testSources before runEval() | nodejs-dev | unit test: `eval-prefilter.test.ts` GREEN | `services/eval-service.ts` |

---

## Dependencies

```
V1-M12 (DONE) ──► V1-M12.1 (this)
```

No external dependencies. All types, data files, and filter function already exist from V1-M12.

---

## Предусловия среды

- [x] npm install в engine/core
- [x] Feature branch: `feature/V1-M12.1-eval-prefilter`
- [x] RED тесты закоммичены

---

## Verification

| Step | Command | Expected |
|------|---------|----------|
| 1 | `npx vitest run eval-prefilter` | 4 RED → GREEN |
| 2 | `npx vitest run` | All 2261+ GREEN |
| 3 | `cd engine/core && npx tsc --noEmit` | 0 errors |
