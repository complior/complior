# V1-M30.7: Rendering Bug Hotfix (4 critical UI bugs before v1.0.0)

> **Status:** 🔴 RED — RED tests pending, awaiting nodejs-dev
> **Branch:** `feature/V1-M30.7-rendering-bugs` (from dev post-V1-M30.6)
> **Created:** 2026-04-30
> **Author:** Architect
> **Triggered by:** True 100% deep verification (per-tab content read on 3 profiles, not regex) found 7 bugs + 4 UX issues. User chose Option C: fix the 4 critical UI rendering bugs now, defer 3 semantic bugs to v1.0.1.
> **Predecessor:** V1-M30.6 fria-regression-passport-hints-ts
> **Successor:** Final /deep-e2e → version bump 0.10.0→1.0.0 → CHANGELOG → PR dev→main → tag v1.0.0

---

## 1. Goal — close 4 critical UI rendering bugs

User did a real per-tab content read of all 3 profiles' HTML reports. Beyond the
already-verified V1-M30.{1..6} fixes, the following 4 UI rendering bugs surfaced
on every profile:

| # | Bug | Location | Real evidence |
|---|---|---|---|
| **W-1** | Passport completeness shows `22%%` (double percent) | `html-renderer.ts:1122` `${pct(p.completeness)}%` — `pct()` already appends `%` | Profile A/B/C all show `22%%` × 2 in passports tab |
| **W-2** | Passport completeness denominator is **/37** but spec says **/36** | `passport-status.ts:23-36` `KNOWN_FIELDS` array contains 37 items, but `html-renderer.ts:1095` intro says `/ 36 total` | Profile A/B/C all show `Completeness: 8/37 fields`. Either array has one extra OR intro is wrong. |
| **W-3** | Action with `command: 'manual review'` rendered as `<code>manual review</code>` — not a real CLI invocation | `priority-actions.ts:107` hardcodes `command: 'manual review'`; `html-renderer.ts` Actions tab renders verbatim | Profile A/B/C show 1-2 `<code>manual review</code>` cells |
| **W-4** | Skipped scanner findings appear in "Available Fix Plans" list | `html-renderer.ts` Fixes tab does not filter `Skipped:` messages out of the available section | Profile A: 4 "Skipped:" entries in Available list, B: 1, C: 2 |

## 2. Tasks Table

| # | Task | Owner | RED test target |
|---|------|-------|-----------------|
| W-1 | Remove double-`%`: replace `${pct(...)}%` with `${pct(...)}` (pct already adds %) at the 2 occurrences in passports tab (`html-renderer.ts:1122`, `:1155`) | nodejs-dev | `passport-completeness-display.test.ts` — assert HTML contains `22%</div>` and `<span class="pp-field filled">Completeness: 22%</span>` (single %) |
| W-2 | Reconcile passport field count: either remove ONE field from `KNOWN_FIELDS` to match spec's 36 OR update intro string to say `/ 37 total`. **Choose**: keep 37 fields (data wins) and update intro string. JSDoc comment explains why 37. | nodejs-dev | `passport-completeness-display.test.ts` — intro says `/ 37 total` AND completeness shows `8/37 fields` AND no `/ 36` mismatch |
| W-3 | In Actions tab, when `a.command === 'manual review'` (or other non-CLI placeholder), render the cell as `<span class="muted small">manual review</span>` instead of `<code>...</code>`. The `<code>` semantic implies a runnable shell command. | nodejs-dev | `actions-manual-review-rendering.test.ts` — fixture with `command: 'manual review'` → HTML contains `<span class="muted small">manual review</span>` and does NOT contain `<code>...manual review...</code>` |
| W-4 | In Fixes tab, filter out fix plans whose `message` starts with `Skipped:` from the "Available Fix Plans" list. Optionally render them in a separate collapsed `<details>` "Not applicable to your profile" section so the user understands they were considered. | nodejs-dev | `fixes-skipped-not-in-available.test.ts` — fixture with 2 fix plans (1 normal + 1 with `message: 'Skipped: ...'`) → "Available" section contains 1 entry; if separate "Not applicable" section rendered, it contains 1 |

## 3. Architecture Requirements (mandatory)

- **Pure render functions** — no side effects, output deterministic given same input
- `Object.freeze` on any new helper objects/arrays
- No public type changes (`PassportDetail`, `PriorityAction`, `FixPlan` etc.)
- `pct()` helper itself unchanged (already correct, just stop appending double `%`)
- KNOWN_FIELDS array unchanged in size (37 stays — fixing intro string instead so we don't break passport.completeness data)
- Manual-review rendering helper named (e.g. `renderActionCommand(cmd)`) and unit-tested
- Skipped filter applied to ALL three sources (`risk-level-filter`, `role-filter`, `domain-filter`) by checking message prefix `Skipped:`
- DO NOT touch any V1-M30.* test file
- DO NOT touch Rust CLI

## 4. Acceptance Criteria

- [ ] All 4 RED tests GREEN
- [ ] /deep-e2e re-run on 3 profiles — Profile A shows:
  - Passport completeness `22%` (single %, NOT `22%%`)
  - Passport `Completeness: 8/37 fields` AND tab intro says `/ 37 total`
  - Actions with manual review rendered as muted span, not `<code>`
  - Fixes "Available Fix Plans" contains 0 `Skipped:` entries
- [ ] Full unit suite stays GREEN (2459+ pass)
- [ ] tsc + cargo fmt + clippy clean
- [ ] dev CI green after merge

## 5. Out of Scope (deferred to v1.0.1)

- **Bug 5**: Eval false negatives — deterministic pattern matcher misses correct AI responses. Requires eval test corpus rework.
- **Bug 6**: `l5-doc-fria` HIGH severity for limited-risk profiles. Scanner check filtering by profile is a deeper change.
- **Bug 7**: `undeclared-permission` reports "No passport found" when passports exist. Scanner state mismatch.
- **UX**: Action title truncation, doc Modified timestamp meaning, disclaimer prose, cross-domain obligation clarity.

These are tracked as TD-63..TD-69 (see tech-debt.md after merge).

## 6. Handoff

After all 4 GREEN → reviewer subagent → architect /deep-e2e re-run → if all 4 fixes
confirmed in real HTML → version bump 0.10.0→1.0.0 across 5 manifests → CHANGELOG
for v1.0.0 (consolidating V1-M20…V1-M30.7) → STRATEGY.md Phase 1 ✅ DONE → PR
`dev → main` → user merges → tag `v1.0.0` → release.yml builds 5 targets +
publishes to crates.io + npm.

This time: architect commits spec + RED tests + STOPS. User spawns nodejs-dev.
After dev done, user spawns reviewer. After APPROVED → user merges. Architect
proceeds with version bump + CHANGELOG only after merge.
