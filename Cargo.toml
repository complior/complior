[workspace]
resolver = "2"
members = ["cli"]

[workspace.package]
version = "1.0.0"
edition = "2024"
license = "AGPL-3.0-only"
repository = "https://github.com/complior/complior"

[profile.release]
lto = true
codegen-units = 1
strip = true
opt-level = "s"
panic = "abort"
